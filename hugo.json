{
  "baseURL": "https://gomatic.github.io/docs.go-optional/",
  "title": "go-optional",
  "publishDir": "_site",
  "enableRobotsTXT": true,
  "markup": {
    "goldmark": {
      "renderHooks": {
        "link": { "useEmbedded": "fallback" },
        "image": { "useEmbedded": "fallback" }
      }
    }
  },
  "params": {
    "description": "Public documentation for gomatic/go-optional — a generic implementation of the functional-options pattern for Go."
  }
}
